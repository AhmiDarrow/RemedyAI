# Old-Remedy — sibling product note

This tree is the **multi-stack RemedyAI product** (Python FastAPI, Tauri desktop, Vite SPA, Smol packaging, full gateway, SQLite memory as authority).

## Status

**This project is active — not frozen.** Treat this repo as product authority for the multi-stack desktop/partner line. Continue development, tests, releases, and agent work here unless the owner says otherwise.

## Sibling path (different product)

`C:\Users\Administrator\Remedy` is a **separate** product (machine-only / RDNA host). It is:

- **Not** a branch, fork, or continuation of this tree
- **Not** a replacement authority for this codebase
- Different bios home (`~/.remedyai` there vs `~/.remedy` here)

Do not merge the two products, share data dirs by default, or treat edits in one as applying to the other.
