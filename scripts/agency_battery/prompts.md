# Live battery prompts

## B1 — Multi-file feature
Implement a small module `sample_util.py` with `add(a,b)` and tests in `test_sample_util.py`. Prefer file_edit/file_write. Run pytest on those tests.

## B2 — Work alone
Handle this on your own: create a README section documenting sample_util and ensure tests pass. Do not ask clarifying questions unless blocked.

## B3 — Fix until green
Introduce a deliberate failing test, then fix the code until pytest is green. Use mission_start/verify.

## B4 — Grep refactor
Rename function `add` to `add_numbers` across the project using repo_search and file_edit.

## B5 — Web (optional)
With web_tools_enabled: fetch https://httpbin.org/get and summarize status.
