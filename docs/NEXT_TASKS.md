# Next tasks

## Done (2026-08-20 session)

- [x] Finish Claude leftover agents (atomic writes, narrowband FIR, host-session exit, smart-turn install)
- [x] Settings Voice section; honest ~9 MB / v3.2 pin; no silent reminder/notify/fault saves
- [x] WebUI/Studio voice (mic, speak-back, Grove → Voice settings); CLI explicitly out of scope
- [x] README test-count is live `~N` only
- [x] Compaction (`compact-and-learn`) this session

## Next (when picked up)

1. **See it:** Settings → Voice HQ; Settings → Phone; ask Remedy in chat to set up calling (`phone_status`). Loopback exists; a real SIP trunk is still Phase 1.
2. Do **not** push `master` unless the owner asks. Tree is local-ahead.
3. Optional later: Kroger live shopping; native press-hold outside the browser rail.

## Parking lot

- Stale git stash `tmp-check` (Aug 3, custom local providers) — superseded by current `LlamaCppProvider`; leave for owner.
- numpy not in base env; smart-turn tensor tests skip without `[voice]`.
