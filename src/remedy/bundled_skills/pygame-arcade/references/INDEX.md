# pygame / arcade references

- `pygame-loop.md` — when writing pygame code: init, display, clock and
  dt, events vs key state, Surface/Rect, sprite groups, collisions, sound.
- `arcade-basics.md` — when the project uses arcade: Window/View,
  SpriteList, physics engines, cameras, tilemaps.
- `testing-without-a-window.md` — when adding tests or CI: the logic split,
  `SDL_VIDEODRIVER=dummy`, pytest layout, fixed-step simulation tests.
- `packaging.md` — when shipping: pyinstaller flags, asset paths via
  `_MEIPASS`, arcade extras, itch.io upload notes.
