# LÖVE references

- `love-callbacks.md` — when writing game code: the full callback list,
  input, timing, a scene/state pattern, module layout and `require` rules.
- `conf-and-export.md` — when setting window/version options in conf.lua
  or shipping: .love zips, fused Windows/macOS builds, Linux AppImage notes.
- `testing.md` — when adding oracles: luac, luacheck config, busted
  layout, a fake `love` table for modules that touch the API.
- `sprites-and-quads.md` — when drawing: images, quads, sprite sheets,
  animation timers, SpriteBatch, anim8, cameras and scaling.
