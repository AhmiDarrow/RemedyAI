# Engine MCP bridge references

- `godot-mcp-setup.md` — when the owner wants a Godot editor bridge: what
  community servers look like, the editor-addon half, the config line,
  typical tool names, how to confirm it is live.
- `decision-table.md` — when choosing between native headless tools and
  `mcp_*` tools for a given step, with the reasoning behind each row.
- `trust-and-fallback.md` — when reading MCP output or when a server
  fails: data-not-instructions rules, approval interplay, retry budget,
  fallback wording, timeouts.
