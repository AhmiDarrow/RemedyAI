# Unity references

- `batchmode-cli.md` — when you need to run the editor headless: compile
  checks, `-executeMethod`, the build script template, log reading, timeouts.
- `project-layout.md` — when you first open a project or must decide where a
  new script, asset, asmdef or editor tool belongs.
- `test-runner.md` — when writing or running Unity Test Framework tests
  (EditMode/PlayMode), asmdef wiring, NUnit XML results.
- `scripting-patterns.md` — when writing C#: MonoBehaviour lifecycle,
  ScriptableObjects, input, coroutines, common mistakes.
